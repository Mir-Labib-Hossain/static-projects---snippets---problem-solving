#include <stdio.h>
int main()
{
    float a;
    printf("Enter the value of Radius to calculate the area of square:");
    scanf("%f",&a);
    printf("Area of square:%.2f",a*a);
    return 0;
}
